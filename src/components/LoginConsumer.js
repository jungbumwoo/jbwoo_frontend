import React from "react";
import { render } from "@testing-library/react";

const LoginConsumer = (props) => {
    // 여기서 컨슈머를 받아온걸
    // 로그인처리하고
    // 그 담에 로그인으로 넘겨주자

    render() {
        return(
            <>
                <div>
                    
                </div>
            </>
        )
    }
}